[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "engineering-data-pipeline"
version = "0.1.0"
description = "Projeto Final de PySpark - MBA FIAP"
dependencies = [
    "pyspark>=3.0.0",
    "pytest>=7.0.0"
]
